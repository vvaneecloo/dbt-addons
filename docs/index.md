# dbt-addons

A CLI built on top of dbt that adds features to your dbt workflows.

## Features

- ✅ [`dbt WAP`](wap/concept.md) : a super simple way to introduce `blue-green deployments` to your pipelines
